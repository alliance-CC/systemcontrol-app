# CLAUDE.md — ツール管理システム

社内のツール／API／認証情報を一元管理する Web アプリ。あわせて各ツールの死活監視と、管理システム自体の稼働監視を行う。
**本システムは実質的に認証情報（シークレット）の集約先である。機密情報の暗号化・アクセス制御・監査を最優先とする。**

詳細仕様は `ツール管理システム_要件定義書_v3.md` を参照（本ファイルは要点と実装方針のみ）。人間側の事前セットアップは `SETUP.md` を参照し、**完了済みとして扱う**。

---

## 技術スタック（確定）

- **フロント／バックエンド**：Next.js（App Router）／TypeScript
- **データストア**：Google スプレッドシート（Google Sheets API、サービスアカウント接続）
- **認証セッション**：iron-session（署名付き HTTP-only Cookie）
- **ステータス保存**：スプレッドシートに書き戻し（`last_status` / `last_checked_at` 列）。KV は使わない
- **定期実行**：外部スケジューラ（UptimeRobot 等）から API ルートを叩く。**Vercel Cron は使わない**（無料 Hobby は1日1回までのため）
- **ホスティング**：Vercel（無料 Hobby プラン）
- **ソース管理**：GitHub

---

## セキュリティ上の必須ルール（絶対厳守）

1. **パスワードを平文で保存しない**。アプリのログインパスワード（タブ3）は bcrypt または argon2 でハッシュ化し、ハッシュ照合でログインする。
2. **保管する認証情報（タブ2のパスワード等）は AES-256-GCM で暗号化してから `details_json` に格納**する。格納形式は `enc:v1:<base64>`（プレフィックスで暗号化済み・鍵バージョンを判別）。表示時に復号する。
3. **シークレットはコードやリポジトリに書かない**。サービスアカウント鍵・暗号化鍵・セッションシークレット・スケジューラ用トークンはすべて環境変数から読む。`.env` はコミットしない（`.gitignore` に追加、`.env.example` のみコミット）。実際の鍵の値は生成・想定しない（人間が SETUP.md で設定する）。
4. **全ページ・全 API ルートでセッションを検証**する。未認証はログイン画面へリダイレクト。例外は `/login` と `/api/health` のみ。
5. **Google Sheets への書き込みは `batchUpdate` でまとめる**（レート制限：サービスアカウントは実質毎分60回）。一覧・検索は毎リクエストでライブの Sheets を叩かず、キャッシュ経由で行う。
6. **ヘルスチェック URL はユーザー入力＝SSRF に注意**。監視前に http/https 限定・プライベート IP 帯／localhost ブロックの検証を通す。
7. **`/api/health` と `/api/patrol` の露出**：`/api/health` は詳細エラーを返さず最小限（200／500）に留める。`/api/patrol` は `CRON_SECRET` 等のトークンで保護し、無認可の呼び出しを弾く。
8. ログイン試行に**レート制限**（総当たり対策）を設ける。

---

## データモデル早見表（詳細は仕様書 §3）

スプレッドシート3タブ。可変項目は JSON、横断的な運用列（ID・監視・監査・検索）は列に昇格。

- **タブ1 マスターデータ**：`category`（大項目） / `subcategory`（小項目） / `google_account`。登録フォームの選択肢用。
- **タブ2 管理データ**（1行＝1レコード）：`id`(UUID・主キー) / `system_name` / `google_account` / `category` / `subcategory` / `details_json`（ツール固有項目。機密値は暗号化） / `health_check_url` / `last_status`(up/down/unknown/none) / `last_checked_at` / `search_blob`（機密を除いた検索用連結文字列） / `created_at` / `updated_at` / `created_by` / `updated_by`。
- **タブ3 アプリユーザー**：`login_id` / `password_hash`（平文不可） / `role`(admin/viewer) / `created_at`。

`details_json` の許可キーは**小項目ごとにアプリ設定（TypeScript）で定義**し、フォームの動的生成とバリデーションの両方に使う（仕様書 §3 のフィールドスキーマ参照）。定義にないキーは拒否する。

---

## 実装方針・規約

- **ルーティングは id で引く**。詳細ページは `/system/[id]`（表示名は URL に使わない）。system_name は表示専用。
- **CRUD をすべて実装**：新規登録（Create）だけでなく編集（Update）・削除（Delete）も。更新時は `updated_at` / `updated_by` を更新。削除は確認ダイアログ。
- **バリデーション**：必須項目、許可キー、同一 `system_name × subcategory × google_account` の重複検知。
- **検索**：起動時／定期的に全データをキャッシュ（メモリ）へ読み込み、`search_blob` に対して部分一致。逆引き（Google アカウント→システム/ツール）も同じ経路。JSON 生値を直接なめない。
- **監視ジョブ（`/api/patrol`）**：対象 URL を `Promise.allSettled` で並列 ping、各リクエストに個別タイムアウト（例5秒）。結果を `batchUpdate` で書き戻す。判定：200=🟢／接続失敗・タイムアウト・5xx=🔴／3xx・401 等の曖昧なもの=🟡／URL 未設定=⚪。
- **秘密の取り扱い**：クライアントへ機密値を送るのは「クリックして表示」操作時のみが望ましい。初期表示ではマスクする。
- **エラーハンドリング**：Sheets 呼び出しは 429 に備えて指数バックオフ。

---

## 推奨ビルド順（段階的に。各フェーズで動作確認 → コミット）

- **フェーズ0 — 土台**：Next.js（App Router, TS）雛形。環境変数の読み込み、Google Sheets クライアントの初期化、暗号化／ハッシュのユーティリティ関数、`.env.example` 作成。
- **フェーズ1 — 疎通**：`/api/health`（Sheets と通信できるか確認、詳細エラーは返さない）。ここで Sheets 接続が通ることを確認。
- **フェーズ2 — 認証**：iron-session、`/login`（タブ3照合）、全ルート保護のミドルウェア、ログイン試行レート制限。
- **フェーズ3 — 登録（Create）**：データモデル、フィールドスキーマ駆動の動的フォーム、暗号化格納、バリデーション（必須・許可キー・重複）。
- **フェーズ4 — 閲覧・検索・編集・削除**：一覧（カード）、統合検索＋逆引き（キャッシュ＋`search_blob`）、詳細ページ（id ルーティング、クリックで表示）、Update／Delete。
- **フェーズ5 — 死活監視**：`/api/patrol`（並列 ping＋個別タイムアウト＋SSRF 対策＋`batchUpdate` 書き戻し、トークン保護）、ダッシュボードのシグナル表示（🟢🔴🟡⚪、1つでも down なら警告バッジ）。外部スケジューラの設定は人間側（SETUP.md）。

---

## v1 のスコープ外（将来）

- ツールが down に変化した際の Webhook 通知連動（実装容易な fast-follow）。
- 権限分離の実運用（v1 は全員 admin。`role` 列は用意済み）。
- ステータス保存の KV 化（性能が必要になったら）。
