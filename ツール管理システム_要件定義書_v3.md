# ツール管理システム 要件定義書（改訂版 v3）

> 本書は初版の要件定義書に、設計レビューで挙がった指摘（機密情報の暗号化・安全な保管、安定した主キー、CRUD、認証セッション設計、死活監視の実装詳細、レート制限対策など）を反映した改訂版です。v3 では Vercel 無料（Hobby）プランでの運用を前提に、定期実行・認証・保存先などの方式を確定させました（§12）。初版からの主な変更点は末尾にまとめています。

---

## 1. システム概要

社内で作成しているアプリやシステムに利用しているツール、API、およびプロジェクトの認証情報（ID/パスワード等）を一元管理するための Web アプリケーション。あわせて、登録した各ツールの死活監視と、管理システム自体の稼働監視を行う。

**本システムが扱うデータの性質**：本システムは実質的に認証情報（シークレット）の集約先であり、単一の漏洩が全システムの資格情報流出につながる。したがって設計上、機密情報の暗号化・アクセス制御・監査が最優先事項となる。

---

## 2. 技術スタック

| 領域 | 採用技術 | 備考 |
|---|---|---|
| フロント / バックエンド | Next.js (App Router) | |
| データストア | Google スプレッドシート (Google Sheets API) | 本来のDBではない制約に留意（§10） |
| 認証セッション | iron-session（決定） | §5。ユーザー名/パスワード照合のみなので外部プロバイダ不要 |
| ステータスキャッシュ | スプレッドシート書き戻し（決定） | last_status / last_checked_at 列に保存。追加インフラ不要。§10 |
| 定期実行 | 外部スケジューラ（UptimeRobot 等）（決定） | 無料プランでは Vercel Cron が使えないため。§10 |
| ホスティング | Vercel（無料 Hobby プラン） | 下記「無料プラン利用時の注意」参照 |
| ソース管理 | GitHub | サービスアカウント鍵・暗号化鍵はコミットしない |

### 無料プラン利用時の注意（Vercel Hobby）

本システムは Vercel の無料 Hobby プランで運用する。以下2点に留意する。

- **商用・業務利用の扱い**：Hobby は規約上「個人の非商用プロジェクト」向けで、業務利用は本来 Pro（月 $20/席）が対象。社内ツールはグレーな位置づけのため、規約範囲外と判断される可能性を理解したうえで利用する。安全側に倒す場合は Pro への移行、または別ホスティング（Railway 等）を検討する。
- **機密データと学習利用**：Hobby で送信されたコンテンツはモデル学習に利用される場合がある（設定でオプトアウト可能）。認証情報を扱うシステムのため、デプロイ前に Vercel アカウント設定で**学習利用をオプトアウト**し、現行規約を確認する。なお保管データは暗号化してスプレッドシート側に置くため、Vercel を通るのは実行時の一時データに限られる。

---

## 3. データベース設計（スプレッドシート構成）

初版の3タブ構成を踏襲しつつ、運用に必要な列を追加する。可変項目を JSON で格納する方針は維持するが、横断的に使う運用列（ID・監視・監査・検索）は JSON から列に昇格させ、検索・監視・編集を安定させる。

### タブ1：マスターデータ（選択肢の記憶用）

新規登録フォームのプルダウン選択肢として読み込む。

| 列名 | 内容 |
|---|---|
| category | 大項目（ツール, API など） |
| subcategory | 小項目（AWS, Figma など） |
| google_account | Google アカウント（〇〇@gmail.com など） |

### タブ2：管理データ（メインデータ）

各システムに紐づくツール／アカウント情報の実データ。1行＝1レコード。

| 列名 | 内容 | 備考 |
|---|---|---|
| **id** | レコードの一意識別子（UUID） | **新規**。行番号は挿入・削除でずれるため、編集・削除・URL は必ずこの ID で行う |
| system_name | システム名（例：社内ポータル） | 表示用 |
| google_account | Google アカウント（大項目とセット） | |
| category | 大項目（ツール, API など） | |
| subcategory | 小項目 / ツール名 | |
| details_json | 追加項目データ。ツール固有の可変項目を JSON で格納 | 機密値は暗号化して格納（§4） |
| **health_check_url** | ヘルスチェック用 URL | **JSON から列に昇格**。監視ジョブが全行の URL を1回の読み取りで取得できるようにするため。未設定可 |
| **last_status** | 最終確認ステータス（up / down / unknown / none） | 監視ジョブが更新 |
| **last_checked_at** | 最終確認日時（ISO 8601） | |
| **search_blob** | 検索用の連結テキスト | **新規**。機密値を除いた検索対象フィールドを連結して保持し、横断検索を高速・安全にする（§8） |
| created_at / updated_at | 作成・更新日時 | 監査用 |
| created_by / updated_by | 作成・更新者（ログイン ID） | 監査用 |

### タブ3：アプリユーザー（ログイン管理用）

| 列名 | 内容 | 備考 |
|---|---|---|
| login_id | ログイン ID | |
| **password_hash** | パスワードのハッシュ（bcrypt または argon2） | **平文保存はしない** |
| role | 権限（admin / viewer） | v1 では全員 admin でも可だが列は用意 |
| created_at | 作成日時 | |

### 追加項目データ（details_json）のスキーマ

可変とはいえ無制限だとキー名のタイプミスでデータ不整合が起きるため、小項目（ツール種別）ごとに「許可するキー」をアプリ側の設定で定義し、**登録フォームの動的生成**と**バリデーション**の両方に使う。この定義はシートを複雑にしないためコード側（設定）に持たせる。

**機密値の扱い**：パスワード等はアプリ側で AES-256-GCM 等で暗号化し、`enc:v1:<base64>` の形式で格納する（先頭のプレフィックスで暗号化済み・鍵バージョンを判別）。復号鍵は Google サービスアカウント鍵とは別の Vercel 環境変数で管理する。

```ts
// 例：ツール種別ごとに許可フィールドと機密フラグを定義（アプリ設定）
export const fieldSchemas = {
  "AWS": [
    { key: "account_name",     label: "アカウント名",       secret: false },
    { key: "account_id",       label: "アカウントID",       secret: false },
    { key: "account_password", label: "パスワード",         secret: true  },
    { key: "notes",            label: "備考",               secret: false },
  ],
  "Figma": [
    { key: "project_name",     label: "プロジェクト名",         secret: false },
    { key: "project_password", label: "プロジェクトパスワード", secret: true  },
  ],
  // ...小項目ごとに追加
} as const;
```

```json
// 格納例（機密値は暗号化済み）
{
  "account_name": "prod-admin",
  "account_id": "123456789012",
  "account_password": "enc:v1:9pKq...base64...==",
  "notes": "本番環境用"
}
```

---

## 4. セキュリティ・権限要件

初版のサービスアカウント権限・アプリアクセス制限に加え、機密情報の保護を明確化する。

- **アプリのログインパスワード（タブ3）**：bcrypt / argon2 でハッシュ化して保存し、ログイン時はハッシュ照合する。平文で保存しない。
- **保管する認証情報（タブ2のパスワード等）**：アプリ側で暗号化（AES-256-GCM 推奨）してから格納。スプレッドシート本体が流出しても復号鍵がなければ読めない状態にする。
- **鍵管理**：Google サービスアカウント鍵（JSON）と暗号化鍵は、いずれも Vercel 環境変数に格納し、GitHub にコミットしない（.gitignore 徹底）。両鍵とも定期ローテーションを運用ルール化する。**サービスアカウント鍵が漏れると設計上すべてのデータにアクセス可能になるため、最重要の秘密として扱う。**
- **スプレッドシートの権限**：Sheets API 接続用サービスアカウントのみに編集者権限を付与し、人間の直接アクセスは付与しない。
- **「クリックして表示」**：覗き見防止の UX 機能であり、値はブラウザまで復号後の平文で届く。実データの保護は上記の暗号化で担保する。可能なら詳細ページ初期表示時は機密値をサーバーから送らず、表示操作時に都度取得・復号する方式にすると、漏洩面をさらに狭められる。
- **アプリ全体のアクセス制限**：ログイン機能（§5）に加え、必要に応じて Basic 認証・社内 IP 制限を併用。ただしヘルスチェック API は外部監視から到達できるよう除外する（§9・§10）。

---

## 5. 認証・セッション設計（新規）

初版はタブ3との照合までを定義していたが、ログイン後の状態保持が未定義だった。以下を追加する。

- **セッション保持**：署名付き HTTP-only Cookie（iron-session）または Auth.js を用いる。すべてのページ・API ルートでセッションを検証し、未認証はログイン画面へリダイレクトする（ログイン画面以外を素通りさせない）。
- **総当たり対策**：ログイン試行にレート制限を設ける（例：同一 ID / IP で一定回数失敗したら一時ロック）。
- **権限分離**：閲覧のみ（viewer）と管理（admin）を分けられる設計にする。v1 では全員 admin でも可だが、role 列と判定箇所は用意しておく。

---

## 6. 画面・UI設計

3画面構成は踏襲。ルーティングと編集・削除導線を追加する。

### ① ログイン画面 (/login)
タブ3と照合して認証。成功時はセッションを発行（§5）。

### ② システム一覧・統合検索画面 (/)
- 登録システムをカード形式で一覧表示。
- **統合検索バー**：システム名・Google アカウント・ツール名に加え、追加項目データ内の値も横断検索。検索は §8 の search_blob／キャッシュを対象に行う。
- **逆引き検索**：特定の Google アカウントがどのシステムのどのツールで使われているかを引ける。
- **カードのステータス集約**：1システム＝複数ツールのため、ステータス表示の集約ルールを定義する（例：ツールごとにアイコンを並べる、または1つでも down ならカードに警告バッジ）。

### ③ システム詳細ページ (/system/[id])
- **URL は表示名ではなく id で引く**（表示名はスペース・日本語・重複・改名で壊れるため）。ページ内で system_name を表示。
- 紐づくツール・API・アイパスを一覧表示。details_json を読み込み、値が存在する項目のみ動的表示。
- パスワード等は「クリックして表示」（§4 の注記のとおり、UX 上の覗き見防止）。
- **編集・削除の導線**を各レコードに設置（§7）。

---

## 7. 新規登録・編集・削除フロー

初版は新規登録中心だったが、パスワードのローテーションや廃止ツールの削除は管理ツールの核心のため、更新・削除まで定義する。

**新規登録（Create）**
1. どのシステムに紐付けるかを入力／選択
2. Google アカウントをプルダウン選択／新規追加
3. 大項目（ツール / API）を選択
4. 小項目（ツール名）を選択
5. §3 のフィールド定義に基づき必要項目を動的表示し入力。機密値は暗号化して details_json に格納。id・created_at・created_by 等を自動付与して保存。

**バリデーション**
- 必須項目チェック、フィールド定義にないキーの拒否。
- 重複検知：同一 system_name × subcategory × google_account の重複を警告。

**編集（Update）／削除（Delete）**
- 詳細ページから id を指定して該当レコードのみ編集・削除。updated_at・updated_by を更新。
- 削除は確認ダイアログ＋（可能なら）論理削除やバックアップ併用を推奨。

---

## 8. 検索設計（新規）

- **データソース**：キー入力の都度ライブのシートを叩くとレート制限（§10）に即到達するため、起動時／定期的に全データをキャッシュ（メモリまたは KV）へ読み込み、検索はキャッシュ経由で行う。
- **search_blob**：機密値を除いた検索対象フィールド（system_name, google_account, tool_name, 非機密の追加項目など）を連結した列を持たせ、部分一致検索の精度と速度を上げる。JSON 生値を直接なめる方式はキー名などへの誤マッチを招くため避ける。
- 逆引き（Google アカウント → システム / ツール）も同じキャッシュ・search_blob 上で実現。

---

## 9. 管理システム自体の監視・運用要件

- **ヘルスチェック API**：システムとスプレッドシートが正常通信できるか確認する専用エンドポイント（例：`app/api/health/route.ts`）。**外部公開されるため、詳細なエラー内容は返さず最小限（200／500 程度）に留め、内部情報を漏らさない。** IP 制限を敷く場合も本エンドポイントは監視元から到達可能にする。
- **自動パトロールと通知**：UptimeRobot 等でヘルスチェック API を定期監視。Vercel のダウンや Google API 制限エラーを検知したらメール／Slack・Discord の Webhook で即時アラート。

---

## 10. 登録ツールの死活監視（死活監視ダッシュボード）

初版のキャッシュ表示方針（ダッシュボードは記録済みステータスを即時表示し、その場でのリアルタイム通信テストはしない）は踏襲しつつ、実装上の決定事項を明確化する。

- **定期実行の方式（決定：外部スケジューラ）**：無料 Hobby プランでは Vercel Cron が1日1回までのため、5〜10分間隔の監視には使えない。§9 と同じ外部スケジューラ（UptimeRobot 等）から専用 API ルート（例：`app/api/patrol/route.ts`）を叩く方式に統一する。API ルート自体は通常の HTTP エンドポイントなので、無料プランでも外部から呼び出す分には制限を受けない。ルートは `CRON_SECRET` 等のトークンで保護し、無認可の呼び出しを弾く。
- **ステータスの保存先**：Vercel のサーバーレスはインメモリキャッシュがリクエスト間で保持されない。実質「スプレッドシートに書き戻す（last_status / last_checked_at）」か「Vercel KV / Upstash Redis」の二択。標準はシート書き戻し（追加インフラ不要、更新は5〜10分に1回の `batchUpdate` でまとめて書く）。性能が要る場合は KV を採用。
- **並列実行とタイムアウト**：監視 URL を順番に叩くと1つ遅いだけで関数全体がタイムアウトしうる。`Promise.allSettled` で並列化し、各リクエストに個別タイムアウト（例：5秒）を設ける。Vercel 関数の実行時間上限（既定は短め、Hobby 最大60秒／Pro 最大300秒）にも留意。
- **SSRF 対策**：ヘルスチェック URL はユーザー入力のため、内部リソース（クラウドのメタデータ IP 等）を指すと危険。スキームは http/https 限定、プライベート IP 帯・ローカルホストをブロックする軽い検証を入れる。
- **ステータス判定基準**：
  - 🟢 稼働中：200 OK
  - 🔴 エラー / 停止：接続失敗・タイムアウト・5xx など
  - 🟡 要確認：判定が曖昧なケース。3xx リダイレクトや 401（「認証必須だが生存はしている」）の扱いをここで定義
  - ⚪ 監視対象外：health_check_url 未設定
- **レート制限（Google Sheets）**：読み取り・書き込みとも「プロジェクトあたり毎分300回」「ユーザーあたり毎分60回」で、サービスアカウントは1ユーザー扱い＝実質毎分60回が上限。超過は 429 となり指数バックオフが必要。ダッシュボード表示はキャッシュ参照で回避できるが、監視ジョブの書き込みとユーザーアクセスの競合に注意し、書き込みは `batchUpdate` でまとめる。（超過分は 2026 年後半から Google Cloud の課金対象になる予定。）
- **UI**：一覧カードにシグナルアイコン（🟢/🔴/🟡/⚪）。§6 のカード集約ルールに従う。
- **通知連動（v1：ダッシュボード表示のみ）**：v1 ではステータスを記録・表示するに留める。ツールが down に変化した際に §9 の Webhook へ通知する連動は、実装が容易なため fast-follow（任意）とする。

---

## 11. バックアップ・単一障害点対策（新規）

DB が単一スプレッドシート＝単一障害点。Google の版履歴に加え、定期的なエクスポート／複製を用意する。認証情報の集約先であるため、復旧手順（バックアップからの復元）も運用ルール化しておく。

---

## 12. 設計上の決定事項（デフォルト。後から変更可）

初版で未決だった項目を、無料プラン前提で以下のデフォルトに確定する。いずれも後から変更できる。

| 項目 | 決定 | 備考 |
|---|---|---|
| 定期実行 | 外部スケジューラ（UptimeRobot 等） | 無料プランでは Vercel Cron 不可のため |
| ステータス保存先 | スプレッドシート書き戻し（last_status / last_checked_at） | 追加インフラ不要。性能が要れば将来 KV |
| 認証 | iron-session | 外部プロバイダ不要 |
| 権限分離 | v1 は全員 admin。role 列は用意 | 後から viewer を追加可能 |
| health_check_url | 列に昇格 | 監視ジョブが1回の読み取りで全 URL を取得 |
| ツール down 時の通知 | v1 はダッシュボード表示のみ | Webhook 連動は fast-follow（任意） |
| カードのステータス集約 | 1つでも down なら警告バッジ＋各ツールにアイコン | |
| 機密値の暗号化 | AES-256-GCM、鍵は環境変数、半年ごとにローテーション | |
| バックアップ | Google の版履歴＋週次エクスポート | |

---

## 13. 現場サポートモード（追加要件）

プログラムがわからない現場スタッフが、各ツールの使い方や不具合について**自分の言葉で質問できる**モードを追加する。詳細仕様は `docs/FIELD_SUPPORT.md`。

### 13.1 目的

- 現場が「誰に聞けばいいか分からない」状態を解消し、一次回答をシステム側で返す。
- 開発者への問い合わせを、**開発者がそのまま着手できる依頼文**の形で受け取る（往復のやり取りを減らす）。

### 13.2 参照する資料（ナレッジソース）

| 種別 | 置き場所 | 追加方法 |
|---|---|---|
| ローカル | `knowledge/<ツール名>/*.md` | フォルダを作って Markdown を置く（`knowledge/` 直下は自動検出） |
| GitHub | 任意のリポジトリ | `knowledge/sources.json` に `repo` / `ref` / `include` を追記 |

- 複数のツールを、複数のフォルダ・複数の GitHub リポジトリとして同時に参照できる。
- GitHub の取得結果はメモリに 30 分キャッシュする（Vercel のサーバーレスはディスクへ書けないため）。
- private リポジトリを読む場合は `GITHUB_TOKEN`（Contents: Read-only）を設定する。

### 13.3 機能

1. **質問応答**：資料を見出し単位に分割し、日本語対応の簡易検索（CJK バイグラム + 英数字単語、見出し一致に加重）で関連箇所を抽出。Claude API で現場向けの言葉に噛み砕いて回答し、**根拠にした資料を必ず併記**する。
2. **稼働状況の反映**：登録ツールの `last_status` を回答の材料に含める（機密値は含めない）。
3. **開発者への依頼文作成**：困りごと・対象ツール・再現手順・緊急度から、件名と本文（困っていること／現在の状況／再現手順／期待する動作／影響範囲・緊急度／確認事項／参考資料）を生成する。`DEV_REQUEST_REPO` 設定時は GitHub Issue 作成画面へのリンクを出す。
4. **参照資料の一覧表示**：いま AI が読めている資料と件数、読み込みエラーを画面に常時表示。再読み込みボタンを備える。

### 13.4 制約・非機能要件

- **AI 未設定でも動くこと**：`ANTHROPIC_API_KEY` 未設定、または API 障害時は、該当資料の本文提示にフォールバックする（現場の作業を止めない）。
- **機密の非送出**：AI に渡すのは資料本文とツール名・稼働ステータスのみ。`details_json` の機密値は復号も送信もしない。
- **認証**：`/support` および `/api/support/*` はセッション必須（`/api/support/sync` は `CRON_SECRET` でも可）。
- **参照範囲**：ローカル資料は `knowledge/` 配下に限定し、`../` を含むパスは拒否する。
- **v1 のスコープ外**：ベクトル検索（埋め込み）、Issue の自動起票、質問ログの蓄積・分析。

---

## 初版からの主な変更点

- タブ2に id（UUID）・監査列（created/updated_at, created/updated_by）・search_blob・監視列（last_status, last_checked_at）を追加。health_check_url を JSON から列に昇格。
- タブ3のパスワードを平文からハッシュ（bcrypt/argon2）に変更、role 列を追加。
- 認証情報の保存時暗号化と鍵管理ルールを明文化。
- 認証・セッション設計（§5）を新規追加。
- 詳細ページのルーティングを表示名から id へ変更。
- 新規登録のみだったフローに編集・削除・バリデーションを追加。
- 検索設計（§8）を新規追加（キャッシュ＋search_blob）。
- 死活監視に実装詳細（保存先・並列/タイムアウト・SSRF・判定基準の🟡追加・レート制限・通知連動）を追加。
- バックアップ・単一障害点対策（§11）と未決事項チェックリスト（§12）を新規追加。
- 現場サポートモード（§13）を追加要件として新規追加。
